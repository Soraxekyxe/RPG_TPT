using UnityEngine;

public class AllSoundsScriptAdding : MonoBehaviour
{
    /*
    [SerializeField] private AudioClip pauseSoundClip;
    private AudioSource audioSource;
    audioSource=GetComponent<AudioSource>(); 
    audioSource.clip = pauseSoundClip;
    audioSource.Play();
    */
    
    //a ajouter dans tout les script ou on veut mettre un sound effect
}
